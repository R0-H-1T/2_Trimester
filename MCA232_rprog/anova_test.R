
Ad <- c()
sold <- c()

library(ggplot)
library(corrplot)

cor()
corrplot.mixed (
  aes 
  )

x = c()
y = c()
ans = "10.98 + 2.098 X"

lrmodel() = lm(y~x) # ~ value which you want to predict

plot(y~x,
     
     
abline (lrmodel, lwd = 3, lty = 1, col="black")

names(lmodel)
coef(lrmodel)
summar


predict (lrmodel, newdate = data.frame (x=15))
predict (lrmodel, newdate = data.frame (x=c(15,12, 31)) #for multiple 


x = c(8, 11, 7, 10, 12, 5, 4, 6)
y = c(11,30,25,44,38,25,20,27)
ans 

lrmodel = lm(y~x)
plot(y~x,
     xlab = "Experience",
     ylab = "Rating (Y)",
     main = "Regression Analysis",
     sub = "10.98 + 2.098 X",
     pch = 20,
     cex = 2,
     col = "red")

abline (lrmodel, lwd = 3, lty = 1, col = "black")

coef(lrmodel)
fitted(lrmodel)
summary(lrmodel)
#predict (lrmodel, newdate = data.frame (x = 15 ))
predict (lrmodel, newdate = data.frame (x = c(15) ))

# when to use annova and ths model?
# find something that we can assume and do it - annova
# have value and predict the new ones - (linear regression)prediction model
# have assumption and try to prove it using model = t test
# 
install.packages("remotes")

cars = mtcars
install.packages("https://cran.r-project.org/src/contrib/Archive/prob/prob_1.0-1.tar.gz")

install.packages("https://cran.r-project.org/src/contrib/Archive/prob/prob_0.9-5.tar.gz")
# library(prob)


x = cars$speed
y = cars$dist

View(mtcars)

sxy = sum()

beta-i_hat = sxy / sxx
beta_0_height = mean(y) - beta_i_hat * mean(x)
c(beta_0_hat, beta_i_hat)





